<!-- Asynchronous in Javascript -->
let a = 10;
function demo(users) {
    console.log(users)
};
                                                                const users = getUser(); // undefined
demo(users);